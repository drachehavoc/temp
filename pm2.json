{
    "apps": [
        {
            "name": "etic",
            "script": "src/index.ts",
            "watch": true,
            "ignore_watch": [ "uploads", "downloads" ],
            "instances": "max",
            "exec_mode": "cluster",
            "env": {
                "production": true
            }
        }
    ]
}